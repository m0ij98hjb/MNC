/**
 * DEPRECATED — Camera data is now stored in Firestore.
 *
 * All camera management is done via:
 *   Admin panel:   /admin/cameras
 *   API routes:    /api/cameras/[serial]
 *   Camera viewer: /live-camera/[serial]
 *
 * This file is kept only as a historical reference.
 * It is no longer imported anywhere in the codebase.
 */

export const projects = [];
